import React from 'react';
import PlaceholderPage from '../components/PlaceholderPage';

const Login: React.FC = () => {
  return (
    <PlaceholderPage
      title="Login"
      description="Sign in to your account to access your courses, track progress, and manage your learning journey."
      actionText="Back to Home"
      actionLink="/"
    />
  );
};

export default Login;
