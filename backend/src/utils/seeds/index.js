const { usersData } = require('./usersSeeds');
const { coursesData } = require('./coursesSeeds');
const { testimonialsData } = require('./testimonialsSeeds');
const { internshipsData } = require('./internshipsSeeds');

module.exports = {
  usersData,
  coursesData,
  testimonialsData,
  internshipsData
};
